# Ruchir owns this file (blueprint §4). Placeholder created by Harsh's foundation scaffold.
# PostgreSQL is server-only; never expose in NEXT_PUBLIC_* variables.
DATABASE_URL="postgresql://user:password@localhost:5432/dealflow360?schema=public"
